# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Xcode — user-specific state (never commit)
xcuserdata/
*.xcuserstate
*.xcuserdatad
**/project.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings
*.xcodeproj/*.mode1v3
*.xcodeproj/*.mode2v3
*.xcodeproj/*.pbxuser
*.xcodeproj/*.perspectivev3

# Xcode — build artifacts
build/
DerivedData/
*.xcarchive
*.xcarchive.zip
*.dSYM.zip
*.dSYM

# Release artifacts (DMG/zip output, exported .app, ExportOptions.plist)
release/

# Xcode — legacy / backup files
*.pbxproj.bak*
*.pbxproj.backup

# Swift Package Manager
.swiftpm/
Packages/
Package.pins
.build/

# CocoaPods (not used here, but common)
Pods/
*.xcworkspace/xcshareddata/swiftpm/configuration

# Build logs
build.log
build-cli.log
build-mcp.log

# Test output
*.profraw

# Local signing overrides (developer-specific)
Signing.local.xcconfig

# Editors
.vscode/
.idea/

# Scratchpad (local notes, never commit)
.scratchpad/
